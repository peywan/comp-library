//proptypes = a mechanism that ensures that props
// passed to a component are of the correct type
// age: PropsTypes.number

//default props
//deault props are default values for props in case they are
// not passed to a component from the parent component
// Name: "guest"

/*
import propTypes from 'prop-types';

function Student(props){
    return(
        <div className="student">
            <p>Name: {props.name}</p>
            <p>Age {props.age}</p>
            <p>Is {props.name} a student? Student value: {props.isStudent ? "yes" : "no"}</p>

            <p ></p>
        </div>
    )
}

Student.propType = {
    name: propTypes.string,
    age: propTypes.number,
    isBoolean: propTypes.bool,
}

export default Student
*/
